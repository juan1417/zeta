fn mean($vec) {
    $n = len($vec)
    if ($n == 0) {
        return 0
    }
    $suma = 0
    for ($i in range($n)) {
        $suma = $suma + $vec[$i]
    }
    return $suma / $n
}

fn stddev($vec) {
    return 0
}

fn correlation($x, $y) {
    return 0
}

export { mean }
